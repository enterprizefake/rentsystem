<!-- 未登录 -->
<view wx:if="{{!user}}">
  <van-empty description="请先登录" />
  <button bindtap="login">登录</button>
</view>

<!-- 已登录 -->
<view wx:else>
  <!-- 共有部分 -->

  <!-- 顶部框架 -->
  <view class="{{user_type}}">

    <!-- 头像 -->
    <van-image class="v0" width="120rpx" height="120rpx" round src="{{user.avatarUrl}}" />

    <!-- 昵称 -->
    <view class="v1">
      <view class="v2">
        <text>
          {{user.nickName}}({{ user_type=='tenant'? '租客':'房东'}})
        </text>
      </view>

      <!-- 消息通知和切换 -->
      <view class="v3">
        <text bindtap="change">切换</text>
      </view>
    </view>

  </view>

  <van-cell title="个人资料" is-link bindtap="jump1"></van-cell>
  <van-cell title="我的订单" is-link bindtap="jump2"></van-cell>

  <!-- 租客部分 -->
  <view wx:if="{{user_type=='tenant'}}">
    <van-cell title="我的收藏" is-link bindtap="jump3"></van-cell>
  </view>

  <!-- 房东部分 -->
  <view wx:else>
    <van-cell title="我要出租" is-link bindtap="jump4"></van-cell>
    <van-cell title="房屋管理" is-link bindtap="jump5"></van-cell>
    <van-cell title="租客预约管理" is-link bindtap="jump6"></van-cell>
  </view>

  <!-- 管理员部分 -->
  <view wx:if="{{ (user.type=='小程序管理员')||(user.type=='小程序核心管理员') }}">
    <van-cell title="管理中心(管理员)" is-link
     bindtap="jump7" 
     data-name="{{user.type}}"></van-cell>
  </view>



  <view class="message">
    <van-icon name="chat-o" info="{{user_type=='tenant'?tenent_messages:landlord_messages}}" size="2rem" bindtap="jump8"></van-icon>
  </view>

</view>